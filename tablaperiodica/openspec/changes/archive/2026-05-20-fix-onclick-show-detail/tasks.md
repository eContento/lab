## 1. Corrección del bug

- [x] 1.1 En `app.js` línea 64, dentro de `createCell()`, reemplazar `selectElement(el)` por `showDetail(el)` en el event listener del clic
- [x] 1.2 Verificar en el navegador que al hacer clic en cualquier celda se abre el panel de detalle correctamente
