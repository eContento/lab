## 1. CSS Grid — fila de separación

- [x] 1.1 En `styles.css`, cambiar `grid-template-rows: repeat(9, 64px)` por `repeat(7, 64px) 20px repeat(2, 64px)` en el selector `#periodic-table`

## 2. JavaScript — actualizar números de fila

- [x] 2.1 En `app.js`, en el bloque de lantánidos, cambiar `cell.style.gridRow = 8` por `cell.style.gridRow = 9`
- [x] 2.2 En `app.js`, en el bloque de actínidos, cambiar `cell.style.gridRow = 9` por `cell.style.gridRow = 10`

## 3. Verificación

- [x] 3.1 Abrir `index.html` en el navegador y comprobar que existe un espacio en blanco visible entre la fila 7 de la tabla y los lantánidos
- [x] 3.2 Comprobar que lantánidos y actínidos siguen en sus posiciones correctas (columnas 3–17)
