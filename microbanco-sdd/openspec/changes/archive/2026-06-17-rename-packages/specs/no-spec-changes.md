## NO CHANGES

Esta es una refactorización pura (reempaquetado, renombrado, eliminación de `AccountApplication`, anidamiento de `AccountStatus`). No hay cambios en los requisitos a nivel de especificación. El comportamiento del sistema, la API REST expuesta y los contratos permanecen idénticos.
